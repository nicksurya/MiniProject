package com.testing.miniproject.presentation.detail.component.entities

enum class Rule {
    REQUIRED,
    MIN_LENGTH,
    MAX_LENGTH,
    MIN_VALUE, // Price type only
    MAX_VALUE, // Price type only
    REGEX
}